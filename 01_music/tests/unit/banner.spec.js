import Banner from '@/components/home/Banner.vue';
import { mount } from '@vue/test-utils';

describe('Banner.vue', () => {
  test('renders innet text', () => {
    const wrapper = mount(Banner);
    
  });
});
